{
  "compilerOptions": {
    // This tells TypeScript to include the Deno-specific APIs and globals like `Deno`.
    "lib": ["deno.ns", "dom"],
    // These are standard best practices for modern TypeScript.
    "strict": true,
    "esModuleInterop": true,
    // This is important: it tells the compiler not to generate any output files.
    "noEmit": true
  },
  // This ensures the settings are applied to all your function files.
  "include": ["functions/**/*.ts"]
}